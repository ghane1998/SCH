# پرامپت طلایی برای ساخت کامل سامانه جامع مدیریت مدرسه هوشمند و پیشرفته

اگر می‌خواهید این اپلیکیشن بی‌نظیر را با تمام جزئیات خیره‌کننده، امنیت چندلایه‌ای، و دیتابیس بهینه در سیستم هوش مصنوعی دیگری بسازید، پرامپت زیر را به طور کامل کپی کرده و به آن بدهید:

```text
یک سامانه جامع، بسیار پیشرفته و هوشمند برای مدیریت مدرسه (مخصوص مدارس دوزبانه/فارسی با چینش کاملا راست‌چین RTL) با فریم‌ورک React 18، ابزار ساخت Vite، زبان TypeScript و استایل‌دهی Tailwind CSS به همراه بک‌اند Express.js و لایه ارتباط با دیتابیس Prisma طراحی کنید. این سامانه باید مجهز به موتور دوگانه دیتابیس (ترجیحاً PostgreSQL در محیط ابری و SQLite برای تست محلی بدون کرش) باشد.

سامانه باید از نظر بصری فوق‌العاده شیک، با تایپوگرافی زیبا (فونت‌های فارسی معتبر مانند وزیرمتن)، دارای دکمه‌های کنترلی با انیمیشن‌های روان لرزشی و هاور عالی، و ساختار کاملاً واکنش‌گرا (Mobile-First با بهینه‌سازی دسکتاپ) باشد.

جزئیات فنی و عملکردی بخش‌های مختلف به شرح زیر است:

### ۱. معماری و ساختار دیتابیس (Prisma Schemas)
دیتابیس باید تمام موجودیت‌های زیر را به صورت رابطه‌ای پشتیبانی کند و برای فیلدهای پیچیده از نوع داده JSONB استفاده کند:
- **Admins (مدیران):** شناسه، نام، نام خانوادگی، هش رمز عبور (bcrypt)
- **Teachers (معلمان):** شناسه، نام، نام خانوادگی، لیست کلاس‌های تخصیص‌یافته، هش رمز عبور
- **Students (دانش‌آموزان):** شناسه، نام، نام خانوادگی، شناسه کلاس، نام کلاس، کدملی، تاریخ تولد، جزئیات شناسنامه (JSON)، محل تولد، ملیت، دست برتر (چپ‌دست/راست‌دست)، وضعیت سلامتی و پزشکی (JSON)، اطلاعات خانواده، اطلاعات تماس، پرونده انضباطی، داده‌های طرح نصیب (JSON)، تصویر پروفایل، هش رمز عبور
- **Classes (کلاس‌ها):** شناسه، نام کلاس، معلم راهنما، لیست دروس ارائه شده (JSON)
- **Grades (نمرات):** شناسه نمره، شناسه دانش‌آموز، شناسه معلم، نام درس، نمره ثبت‌شده، تاریخ ثبت
- **Attendance (حضور و غیاب):** شناسه، دانش‌آموز، تاریخ، وضعیت (حاضر/غایب/تاخیر)، میزان تاخیر به دقیقه، ساعت خروج تعجبی، وضعیت اطلاع‌رسانی به خانواده، داشتن گواهی پزشکی، شناسه ثبت‌کننده غیبت
- **Discipline (پرونده انضباطی):** شناسه، دانش‌آموز، تاریخ، دسته‌بندی تخلف، شرح ماجرا، اقدام تنبیهی/تشویقی، گزارش‌دهنده
- **Exams (امتحانات):** درس، تاریخ امتحان، بودجه‌بندی و مباحث امتحان، کلاس‌های هدف، تاریخ اعلام، شرح امتحان، سازنده
- **PTA Meetings (جلسات انجمن اولیا و مربیان):** عنوان، تاریخ، محدوده جلسه (کل مدرسه/پایه/کلاس خاص)، شرح جلسه، ثبت‌کننده
- **PTA Attendance (حضور و غیاب اولیا):** شناسه جلسه، شناسه دانش‌آموز، وضعیت حضور ولی، توضیحات اضافی
- **Bills (شهریه و امور مالی):** شناسه، دانش‌آموز، عنوان قبض، شرح، سال تحصیلی، مبلغ کل، مبلغ پرداخت شده، وضعیت پرداخت (تسویه/بدهکار/بخشی پرداخت شده)، تاریخ صدور، مهلت پرداخت
- **Payments (تراکنش‌های مالی):** شناسه، شناسه قبض مالی، دانش‌آموز، تاریخ، مبلغ واریزی، روش پرداخت (کارت/چک/نقدی)، توضیحات چک (شماره چک، سررسید، بانک صادرکننده در قالب JSON)، ثبت‌کننده
- **Events (رویدادها و مسابقات):** عنوان، شرح، تاریخ و ساعت، مکان، هزینه ورودی، لینک ثبت‌نام، جایزه رویداد، برگزارکننده، مخاطبان هدف (JSON)، سازنده، تصویر رویداد
- **Responsibilities (مسئولیت‌ها):** نام مسئولیت (همیار معلم/شورای دانش‌آموزی و...)، نوع مسئولیت، رنگ متمایز کننده، سازنده
- **Responsibility Assignments (تخصیص مسئولیت):** شناسه مسئولیت، شناسه دانش‌آموز، تاریخ شروع، تاریخ پایان، واگذارکننده
- **Anecdotal Records (مشاهدات رفتار دانش‌آموزان):** لیست دانش‌آموزان (JSON)، تاریخ، موضوع، مکان، شرح رفتار مشاهده‌شده، ثبت‌کننده
- **Parent Meetings (ملاقات با اولیا):** شناسه دانش‌آموز، تاریخ، افراد حاضر، علت دعوت، خلاصه گفتگو، مصوبات جلسه، ثبت‌کننده
- **Badges (نشان‌های افتخار و گیمیفیکیشن):** نام نشان، تصویر نشان، معیارهای کسب نشان، شرح نشان، سازنده، سطح اثرگذاری
- **Awarded Badges (نشان‌های اهدا شده):** شناسه نشان، دانش‌آموز، تاریخ اهدا، اهداکننده، علت کسب نشان
- **Assets (اموال و تجهیزات مدرسه):** شناسه اموال، بارکد فیزیکی، نوع دارایی (کتاب/تبلت/اموال ورزشی)، وضعیت فنی، شرح دارایی
- **Asset Assignments (امانت اموال):** شناسه اموال، دانش‌آموز، تاریخ امانت، تاریخ بازگشت، توضیحات تحویل
- **Notifications (اعلانات سیستم):** عنوان، پیام، مخاطبان هدف (JSON)، تاریخ ایجاد، فرستنده، رنگ اعلان، برچسب‌ها (JSON)، لینک کمکی، فعال بودن
- **Scheduled Notifications (اعلانات زمان‌بندی‌شده):** الگوی عنوان، الگوی پیام، نوع، مخاطبان، رنگ، تصویر، تاریخ انتشار خودکار، وضعیت فعال بودن، سازنده
- **Settings (تنظیمات مدرسه):** نام مدرسه، لوگو، مقطع تحصیلی (دبستان/دبیرستان اول/دبیرستان دوم)، انتخاب تم رنگی سامانه، نوع تقویم (شمسی)

### ۲. سیستم امنیتی ورود (Authentication System)
- ورود به سامانه باید از طریق درخواست امن به اندپوینت `/api/auth/login` در بک‌‌اند باشد.
- فیلدها شامل: شناسه کاربری (User ID)، رمز عبور و نقش انتخابی کاربر (دانش‌آموز، معلم، مدیریت).
- رمزهای عبور کاربران در دیتابیس به صورت کاملاً رمزنگاری‌شده با الگوریتم bcrypt ذخیره می‌شوند. رمز عبور پیش‌فرض تمام کاربران برای سهولت تایید اولیه `123456` است.
- پس از احراز هویت موفق، یک توکن امضاشده JWT تولید شده و به کلاینت بازگردانده می‌شود.
- تمام درخواست‌های بعدی کلاینت باید این توکن را در هدر درخواست به صورت `Authorization: Bearer <token>` ارسال کنند. در صورت خروج (Logout)، توکن پاک شده و صفحه ریلود می‌شود تا سشن قبلی به طور کامل منقضی گردد.

### ۳. سیستم دسترسی چندلایه‌ای و پویای اطلاعات (Layered/Tiered Data Access Control)
اندپوینت‌های سرور باید با استفاده از میان‌افزار احراز هویت (Middleware) به شدت محافظت شوند و بر اساس نقش کاربر، داده‌های خروجی را فیلتر کنند:
- **مدیر سامانه (Admin):** دسترسی ۱۰۰٪ آزاد به خواندن، نوشتن، ویرایش و حذف در تمامی جدول‌ها و اعمال تنظیمات مدرسه.
- **معلم (Teacher):**
  - مجاز به دیدن کلاس‌های تحت تدریس خود و دانش‌آموزان آن کلاس‌ها.
  - مجاز به ثبت، ویرایش و حذف نمرات، حضور و غیاب، پرونده‌های انضباطی، مشاهدات رفتاری، قرار ملاقات با والدین و اهدای نشان‌های افتخار برای دانش‌آموزان کلاس‌های خود.
  - **ممنوعیت مطلق:** معلمان به هیچ عنوان نباید به اطلاعات مالی مدرسه (قبوض شهریه، پرداخت‌ها، مبالغ کل دریافتی) دسترسی داشته باشند و اندپوینت‌های مربوطه برای آن‌ها لیست خالی برگشت می‌دهند.
- **دانش‌آموز (Student):**
  - **ممنوعیت مطلق در نوشتن و حذف:** دسترسی به متدهای POST و DELETE برای نقش دانش‌آموز کلاً مسدود و Forbidden (کد ۴۰۳) است.
  - دانش‌آموز در بخش GET فقط و فقط مجاز به دیدن کارنامه نمرات خود، سابقه حضور و غیاب خود، موارد انضباطی خود، مسئولیت‌های خود، اموال امانت گرفته شده توسط خود، نشان‌های کسب شده توسط خود، و قبوض و پرداخت‌های شهریه شخصی خود است.
  - در بخش رویدادها و اعلانات، فقط مواردی برای دانش‌آموز بارگذاری می‌شود که مخاطب هدف آن رویداد، این دانش‌آموز خاص یا کل دانش‌آموزان مدرسه باشند.

### ۴. ویژگی‌های فرانت‌اند (UI/UX)
- **داشبورد مدیریت:** شامل نمودارهای تحلیلی و آماری حضور و غیاب، گزارش‌های مالی تفکیک شده، بخش مدیریت کادر آموزشی و دانش‌آموزان، کنترل تجهیزات مدرسه و تنظیمات پیشرفته با پشتیبانی از آپلود لوگوی مدرسه.
- **داشبورد معلمان:** پنل آسان برای ثبت نمرات کلاسی و حضور و غیاب سریع با استفاده از تیک‌های سبز/قرمز، ثبت گزارش‌های مشاهداتی دانش‌آموزان، مدیریت جلسات اولیا و مربیان.
- **داشبورد دانش‌آموزان:** ویترین دستاوردهای گیمیفیکیشن، بررسی نمرات، مشاهده قبوض مالی و فیش‌های واریزی، لیست امتحانات آینده و اخبار مدرسه با زبانه اعلانات زنده.
- طراحی باید از تم‌های رنگی پویا، افکت‌های هاور جذاب بر روی کارت‌ها، فونت خوانای سان‌سریف فارسی، دکمه خروج امن و بارگذاری روان داده‌ها استفاده کند.
```

این پرامپت تضمین می‌کند که هر سیستم هوش مصنوعی پیشرفته‌ای بتواند این پروژه را با رعایت اصول مهندسی نرم‌افزار، لایه‌بندی امنیتی دقیق و تطابق کامل با استانداردهای مدرن فرانت‌اند و بک‌اند بازسازی کند.
